interface:
  display_name: "Chrome Control Router"
  short_description: "Prefer Chrome extension, fall back to managed CDP"
  default_prompt: "Use $chrome-cdp-manager to control Chrome through the connected extension, falling back to the managed CDP profile only when needed."
